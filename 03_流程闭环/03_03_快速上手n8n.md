---
title: 快速上手n8n
description: 掌握n8n的本地部署和基本操作，能够搭建本地自动化工作流
---

# 快速上手n8n

> **模块目标**: 掌握n8n的本地部署和基本操作，能够搭建本地自动化工作流

---

## 一、n8n本地部署

### 1.1 n8n是什么

**n8n**（读音"n-eight-n"）是一个开源的工作流自动化工具，可以自托管在本地或私有服务器上。

**核心特点**：
- 💻 **开源免费**：源代码完全开放，self-hosted免费使用
- 🔒 **数据私有**：所有数据存储在本地，完全可控
- 🔧 **高度可定制**：支持自定义节点开发
- 🐳 **易于部署**：Docker一键启动

**为什么选择n8n**：
- 对数据隐私有要求
- 需要与内部系统集成
- 希望降低长期使用成本
- 需要高度定制化

### 1.2 部署方式选择

n8n提供多种部署方式：

| 部署方式 | 难度 | 适用场景 | 推荐指数 |
|---------|------|---------|---------|
| **Docker** | ⭐⭐ | 所有场景 | ⭐⭐⭐⭐⭐ |
| npm安装 | ⭐⭐⭐ | 开发测试 | ⭐⭐⭐ |
| 桌面应用 | ⭐ | 个人体验 | ⭐⭐⭐⭐ |
| 云服务(n8n.cloud) | ⭐ | 不想自己部署 | ⭐⭐⭐⭐ |

> 💡 **推荐**：Docker部署是最稳定、最通用的方式，也是本教程的重点。

### 1.3 Docker部署详细步骤

#### 前置准备

1. **安装Docker Desktop**

Windows/Mac用户：
- 访问 [Docker官网](https://www.docker.com/products/docker-desktop)
- 下载并安装Docker Desktop
- 启动Docker Desktop

验证安装：
```bash
docker --version
# 输出示例：Docker version 24.0.0, build xxxxx
```

2. **创建工作目录**

```bash
# Windows (PowerShell)
mkdir C:\n8n-data

# Mac/Linux
mkdir ~/n8n-data
```

#### 启动n8n

**方式一：快速启动（测试用）**

```bash
docker run -d --name n8n -p 5678:5678 -v C:\n8n-data:/home/node/.n8n n8nio/n8n
```

**方式二：完整配置（推荐生产使用）**

```bash
docker run -d \
  --name n8n \
  -p 5678:5678 \
  -e TZ=Asia/Shanghai \
  -e GENERIC_TIMEZONE=Asia/Shanghai \
  -e N8N_BASIC_AUTH_ACTIVE=true \
  -e N8N_BASIC_AUTH_USER=admin \
  -e N8N_BASIC_AUTH_PASSWORD=your_password \
  -v C:\n8n-data:/home/node/.n8n \
  --restart unless-stopped \
  n8nio/n8n
```

参数说明：

| 参数 | 说明 |
|-----|------|
| `-d` | 后台运行 |
| `--name n8n` | 容器名称 |
| `-p 5678:5678` | 端口映射 |
| `-e TZ=Asia/Shanghai` | 设置时区 |
| `-v C:\n8n-data:/home/node/.n8n` | 数据持久化 |
| `--restart unless-stopped` | 自动重启 |

#### 访问n8n

1. 打开浏览器，访问 `http://localhost:5678`
2. 首次访问需要创建管理员账号
3. 填写邮箱和密码，完成注册

### 1.4 常见问题排查

#### 问题1：端口被占用

```
错误信息：port is already allocated

解决方案：
1. 查看端口占用：netstat -ano | findstr 5678
2. 修改端口：-p 5679:5678
3. 访问地址变为：http://localhost:5679
```

#### 问题2：容器启动失败

```
排查步骤：
1. 查看容器日志
   docker logs n8n

2. 检查Docker是否运行
   docker ps

3. 重新创建容器
   docker rm n8n
   docker run ...（重新执行启动命令）
```

#### 问题3：数据丢失

```
原因：没有挂载数据卷

解决方案：
确保启动命令包含 -v 参数：
-v C:\n8n-data:/home/node/.n8n
```

---

## 二、插件玩法

### 2.1 n8n节点体系

n8n的节点分为三大类：

#### 触发器节点（Trigger）

启动工作流的入口：

| 节点名称 | 说明 | 使用场景 |
|---------|------|---------|
| **Manual Trigger** | 手动触发 | 测试、临时执行 |
| **Schedule Trigger** | 定时触发 | 周期性任务 |
| **Webhook** | 接收HTTP请求 | API对接 |
| **Email Trigger** | 收到邮件时触发 | 邮件处理 |

#### 操作节点（Action）

执行具体任务的节点：

| 节点名称 | 说明 | 使用场景 |
|---------|------|---------|
| **HTTP Request** | 发送HTTP请求 | API调用 |
| **Google Sheets** | 操作表格 | 数据存储 |
| **Code** | 执行代码 | 复杂逻辑 |
| **Set** | 设置变量 | 数据传递 |

#### 逻辑节点（Logic）

控制流程走向：

| 节点名称 | 说明 | 使用场景 |
|---------|------|---------|
| **IF** | 条件判断 | 分支处理 |
| **Switch** | 多条件分支 | 复杂分流 |
| **Merge** | 合并数据 | 汇总结果 |
| **Loop** | 循环处理 | 批量操作 |

### 2.2 常用节点介绍

#### HTTP Request节点

最常用的节点之一，用于调用外部API。

```
配置项：
- Method：GET/POST/PUT/DELETE
- URL：API地址
- Authentication：认证方式
- Headers：请求头
- Query Parameters：查询参数
- Body：请求体

使用示例：调用天气API
- Method：GET
- URL：https://api.weather.com/v1/current
- Query Parameters：city=Beijing
```

#### Webhook节点

接收外部系统的请求，触发工作流。

```
配置项：
- HTTP Method：GET/POST
- Path：路径（如 /my-webhook）
- Authentication：认证方式

使用示例：
1. 添加Webhook节点
2. 设置Path为 /notify
3. 激活工作流后，访问：
   http://localhost:5678/webhook/notify
4. 工作流将被触发
```

#### Code节点

执行自定义JavaScript代码。

```javascript
// 示例：处理输入数据
const items = $input.all();

const processedItems = items.map(item => {
  return {
    json: {
      name: item.json.name.toUpperCase(),
      timestamp: new Date().toISOString()
    }
  };
});

return processedItems;
```

#### Google Sheets节点

读写Google表格数据。

```
操作类型：
- Read：读取数据
- Append：追加数据
- Update：更新数据
- Delete：删除数据

配置步骤：
1. 创建Google Cloud项目
2. 启用Google Sheets API
3. 创建服务账号凭证
4. 在n8n中配置凭证
```

### 2.3 凭证管理

#### 什么是凭证

凭证（Credential）是访问外部服务所需的认证信息，如：
- API密钥
- 用户名密码
- OAuth令牌

#### 如何配置凭证

**步骤1：添加凭证**

1. 进入Settings → Credentials
2. 点击"Add Credential"
3. 选择服务类型（如HTTP Header Auth）
4. 填写认证信息
5. 保存

**步骤2：在节点中使用**

1. 添加需要认证的节点
2. 在"Credential"下拉框中选择已创建的凭证
3. 完成配置

#### 安全注意事项

- ⚠️ 不要在代码中硬编码凭证
- ⚠️ 定期更换API密钥
- ⚠️ 使用最小权限原则
- ⚠️ 备份凭证配置

---

## 三、功能拓展

### 3.1 社区节点安装

n8n有活跃的社区，提供了大量第三方节点。

#### 安装方法

1. 进入Settings → Community Nodes
2. 点击"Install a community node"
3. 输入节点包名（如 `n8n-nodes-chatgpt`）
4. 点击Install

#### 推荐社区节点

| 节点名称 | 功能 |
|---------|------|
| n8n-nodes-chatgpt | ChatGPT集成 |
| n8n-nodes-telegram | Telegram Bot |
| n8n-nodes-wechat | 微信消息推送 |

### 3.2 与外部系统集成

#### 数据库连接

n8n支持多种数据库：

| 数据库 | 节点 | 用途 |
|-------|------|------|
| MySQL | MySQL | 关系型数据库 |
| PostgreSQL | Postgres | 关系型数据库 |
| MongoDB | MongoDb | 文档数据库 |
| Redis | Redis | 缓存数据库 |

#### 文件系统操作

```
文件相关节点：
├── Read Binary File：读取文件
├── Write Binary File：写入文件
├── Read/Write Files from Disk：本地文件操作
└── Google Drive：云存储操作
```

### 3.3 高级功能

#### 错误处理

```
错误处理策略：

1. 节点级别
   - Settings → Continue On Fail
   - 失败时继续执行

2. 工作流级别
   - 添加Error Trigger节点
   - 捕获并处理错误

3. 自定义处理
   - 使用IF节点判断执行结果
   - 失败时执行备选逻辑
```

#### 重试机制

```
配置重试：
1. 节点Settings → Retry on Fail
2. 设置重试次数
3. 设置重试间隔

适用场景：
- 调用不稳定的API
- 网络请求可能失败的情况
```

#### 并发控制

```
控制并发数：
1. 节点Settings → Execute in Batch
2. 设置批次大小
3. 设置批次间隔

适用场景：
- API有速率限制
- 避免对目标服务器造成压力
```

---

## 四、实战案例：自动化日报生成

### 4.1 场景描述

**需求**：每天自动收集数据，生成日报并发送邮件

**数据来源**：
- 销售数据（Google Sheets）
- 用户反馈（表单提交）
- 系统日志（数据库）

**输出**：
- 日报邮件发送给团队

### 4.2 流程设计

```
工作流架构：

Schedule Trigger（每天早上9点）
    ↓
Google Sheets（读取销售数据）
    ↓
Code节点（数据处理）
    ↓
HTTP Request（获取其他数据）
    ↓
Code节点（汇总计算）
    ↓
HTML节点（生成报告）
    ↓
Email节点（发送邮件）
```

### 4.3 逐步实现

#### 步骤1：创建工作流

1. 点击"Add workflow"
2. 命名为"每日日报自动生成"

#### 步骤2：添加定时触发器

```
节点：Schedule Trigger

配置：
- Trigger Interval：Days
- Days Between Triggers：1
- Trigger at Hour：9
- Trigger at Minute：0
```

#### 步骤3：读取销售数据

```
节点：Google Sheets

配置：
- Operation：Read
- Spreadsheet ID：[你的表格ID]
- Range：Sheet1!A1:D100
```

#### 步骤4：数据处理

```javascript
// Code节点：处理销售数据
const sheetsData = $input.all();

// 计算总销售额
let totalSales = 0;
let orderCount = sheetsData.length;

for (const row of sheetsData) {
  totalSales += parseFloat(row.json.amount || 0);
}

// 计算平均值
const avgOrderValue = orderCount > 0 ? totalSales / orderCount : 0;

return [{
  json: {
    totalSales: totalSales.toFixed(2),
    orderCount: orderCount,
    avgOrderValue: avgOrderValue.toFixed(2),
    date: new Date().toLocaleDateString('zh-CN')
  }
}];
```

#### 步骤5：生成HTML报告

```javascript
// Code节点：生成HTML
const data = $input.first().json;

const html = `
<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; }
    .header { background: #4CAF50; color: white; padding: 20px; }
    .content { padding: 20px; }
    .metric { background: #f5f5f5; padding: 15px; margin: 10px 0; border-radius: 5px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>每日销售日报</h1>
    <p>报告日期：${data.date}</p>
  </div>
  <div class="content">
    <div class="metric">
      <h3>总销售额</h3>
      <p style="font-size: 24px; color: #4CAF50;">¥${data.totalSales}</p>
    </div>
    <div class="metric">
      <h3>订单数量</h3>
      <p style="font-size: 24px;">${data.orderCount} 单</p>
    </div>
    <div class="metric">
      <h3>平均客单价</h3>
      <p style="font-size: 24px;">¥${data.avgOrderValue}</p>
    </div>
  </div>
</body>
</html>
`;

return [{ json: { html: html } }];
```

#### 步骤6：发送邮件

```
节点：Send Email

配置：
- From Email：noreply@company.com
- To Email：team@company.com
- Subject：每日销售日报 - {{date}}
- Email Format：HTML
- HTML：{{html}}
```

### 4.4 测试与激活

#### 测试工作流

1. 点击"Execute Workflow"
2. 检查每个节点的输出
3. 确认邮件发送成功

#### 激活定时任务

1. 点击右上角的"Inactive"开关
2. 状态变为"Active"
3. 工作流将按设定时间自动执行

---

## 五、本章小结

### 核心要点回顾

1. **n8n部署**：Docker是最推荐的部署方式
2. **节点体系**：触发器、操作、逻辑三大类节点
3. **凭证管理**：安全存储和管理认证信息
4. **高级功能**：错误处理、重试机制、并发控制

### 自检清单

完成本章学习后，问问自己：

- [ ] 我能独立部署n8n吗？
- [ ] 我了解常用节点的作用和配置方法吗？
- [ ] 我能配置凭证连接外部服务吗？
- [ ] 我能搭建一个完整的自动化工作流吗？
- [ ] 我已经创建了第一个n8n工作流吗？

### 下一步

掌握n8n工作流后，你已具备本地自动化能力：

**→ [workflow交付](03_04_workflow交付.md)**

在下一模块中，你将学习如何将工作流交付给实际业务使用，让自动化真正产生价值。

---

*n8n是本地工作流的最佳选择。掌握它，你将拥有完全可控的自动化能力！*
