---
title: Skills
description: 掌握MCP和Skills的概念，能够创建和使用自定义Skills
---

# Skills

> **模块目标**: 理解MCP协议，掌握Skills的创建、安装和分享方法

---

## 一、MCP入门

### 1.1 什么是MCP

MCP（Model Context Protocol）是Anthropic公司推出的一种开放协议，用于规范AI模型与外部工具、数据源之间的交互。

**通俗理解**：
- MCP就像是一个"通用翻译器"
- 让AI能够理解和使用各种外部工具
- 让不同的AI应用能够共享同一种"技能"

**为什么需要MCP**：

在没有MCP之前：
- 每个AI应用都有自己的插件系统
- 为ChatGPT写的插件无法在Claude中使用
- 开发者需要为每个平台单独开发

有了MCP之后：
- 一次开发，多处使用
- 统一的标准，降低开发成本
- 技能可以方便地分享和复用

### 1.2 MCP的架构

MCP采用客户端-服务器架构：

```
┌─────────────────┐     ┌─────────────────┐
│   MCP Client    │────▶│   MCP Server    │
│  (Claude等AI)   │◀────│  (Skills/Plugin)│
└─────────────────┘     └─────────────────┘
```

**核心概念**：

| 概念 | 说明 |
|-----|------|
| **MCP Client** | AI应用（如Claude Desktop、Claude Code） |
| **MCP Server** | 提供能力的工具（Skills、Plugin） |
| **Transport** | 客户端和服务器之间的通信方式 |

### 1.3 MCP与Claude的关系

Claude是目前对MCP支持最好的AI：

| Claude产品 | MCP支持情况 |
|-----------|------------|
| Claude Desktop | ✅ 完整支持 |
| Claude Code | ✅ 完整支持 |
| Claude网页版 | ❌ 暂不支持 |

> 💡 这也是为什么我们在前一章学习Claude Code的原因——它是使用MCP Skills的最佳环境。

---

## 二、什么是Skills

### 2.1 Skills的定义

Skills（技能）是基于MCP协议的能力封装，通常是**提示词模板的标准化**。

**简单来说**：
- Skills = 提示词模板 + 工具调用能力
- 让AI能够稳定地完成特定任务
- 可以分享给他人使用

### 2.2 Skills的价值

**问题场景**：

你写了一个很好的提示词，用于生成周报：
```
你是一个周报助手。请根据以下信息生成本周工作周报：
- 本周完成的工作：...
- 遇到的问题：...
- 下周计划：...

输出格式要求：...
```

**存在的问题**：
- 每次都要复制粘贴这个提示词
- 换一个人用，可能不知道怎么用
- 提示词修改后，要通知所有人

**使用Skills后**：
- 一键调用，无需复制粘贴
- 标准化输入，降低使用门槛
- 统一更新，所有人自动获得最新版本

### 2.3 Skills与提示词模板的区别

| 对比维度 | 提示词模板 | Skills |
|---------|-----------|--------|
| **使用方式** | 复制粘贴 | 一键调用 |
| **更新维护** | 手动同步 | 自动更新 |
| **输入验证** | 无 | 可以有 |
| **工具调用** | 不支持 | 支持 |
| **分享方式** | 发送文本 | 发布安装包 |

---

## 三、最简单的Skills样式

### 3.1 Skills的基本结构

一个最简单的Skill包含以下部分：

```json
{
  "name": "skill-name",
  "description": "技能描述",
  "version": "1.0.0",
  "command": "执行命令或提示词"
}
```

### 3.2 Hello World示例

让我们创建一个最简单的Skill——"打招呼"：

**目录结构**：
```
hello-skill/
├── skill.json        # 技能配置文件
└── README.md         # 说明文档
```

**skill.json**：
```json
{
  "name": "hello",
  "description": "一个简单的打招呼技能",
  "version": "1.0.0",
  "prompt": "请用友好的语气向用户打招呼，并询问今天有什么可以帮助的。"
}
```

### 3.3 稍微复杂一点的示例

一个"周报生成器"Skill：

**skill.json**：
```json
{
  "name": "weekly-report",
  "description": "根据输入信息生成工作周报",
  "version": "1.0.0",
  "parameters": {
    "type": "object",
    "properties": {
      "completed": {
        "type": "string",
        "description": "本周完成的工作"
      },
      "problems": {
        "type": "string",
        "description": "遇到的问题"
      },
      "plans": {
        "type": "string",
        "description": "下周计划"
      }
    },
    "required": ["completed"]
  },
  "prompt": "你是一个周报撰写助手。请根据以下信息生成一份专业的工作周报：\n\n## 本周完成\n{{completed}}\n\n## 遇到的问题\n{{problems}}\n\n## 下周计划\n{{plans}}\n\n要求：\n1. 语言简洁专业\n2. 突出重点成果\n3. 字数控制在300字以内"
}
```

---

## 四、使用AI完成一个Skills

### 4.1 需求分析

在创建Skill之前，先明确：

| 问题 | 示例答案 |
|-----|---------|
| 这个Skill解决什么问题？ | 自动生成会议纪要 |
| 需要哪些输入？ | 会议内容、参会人员 |
| 期望的输出是什么？ | 格式化的会议纪要 |
| 有什么特殊要求？ | 需要提取待办事项 |

### 4.2 让AI生成Skills代码

**提示词模板**：

```
请帮我创建一个MCP Skill，具体要求如下：

【功能描述】
[描述Skill的功能]

【输入参数】
- 参数1：[说明]
- 参数2：[说明]

【输出要求】
[描述期望的输出格式和内容]

【其他要求】
[任何特殊要求]

请生成完整的skill.json文件内容。
```

### 4.3 实战案例：创建一个周报生成Skill

**第一步：描述需求**

```
请帮我创建一个MCP Skill，用于生成工作周报。

【功能描述】
根据用户输入的工作内容，生成格式化的周报

【输入参数】
- completed: 本周完成的工作（必填）
- ongoing: 进行中的工作
- problems: 遇到的问题
- plans: 下周计划

【输出要求】
1. 标准的周报格式
2. 包含工作概述、详细内容、问题与风险、下周计划
3. 语言简洁专业
4. 控制在500字以内

请生成完整的skill.json文件内容。
```

**第二步：AI生成的结果**

```json
{
  "name": "weekly-report-generator",
  "description": "根据工作内容生成专业的工作周报",
  "version": "1.0.0",
  "author": "your-name",
  "parameters": {
    "type": "object",
    "properties": {
      "completed": {
        "type": "string",
        "description": "本周完成的工作，必填"
      },
      "ongoing": {
        "type": "string",
        "description": "进行中的工作"
      },
      "problems": {
        "type": "string",
        "description": "遇到的问题和风险"
      },
      "plans": {
        "type": "string",
        "description": "下周工作计划"
      }
    },
    "required": ["completed"]
  },
  "prompt": "你是一位专业的职场写作助手，擅长撰写工作周报。\n\n请根据以下信息生成一份工作周报：\n\n## 本周完成\n{{completed}}\n\n## 进行中\n{{ongoing}}\n\n## 问题与风险\n{{problems}}\n\n## 下周计划\n{{plans}}\n\n---\n\n**输出要求**：\n1. 使用专业的商务语言\n2. 结构清晰，层次分明\n3. 突出重点成果和关键数据\n4. 问题部分要给出建议解决方案\n5. 总字数控制在500字以内\n\n**输出格式**：\n# 工作周报\n\n## 本周工作概述\n[一句话概述]\n\n## 详细工作内容\n### 已完成\n- ...\n\n### 进行中\n- ...\n\n## 问题与风险\n- ...\n\n## 下周计划\n- ..."
}
```

**第三步：验证和调整**

- 检查参数定义是否完整
- 检查提示词是否清晰
- 测试输出效果
- 根据需要调整

---

## 五、本地Skills的安装使用

### 5.1 Skills文件结构

一个完整的Skill项目结构：

```
my-skill/
├── skill.json        # 技能配置（必需）
├── README.md         # 使用说明
├── examples/         # 示例文件
│   └── example1.json
└── src/              # 源代码（如果需要）
    └── main.py
```

### 5.2 安装步骤

#### 方法1：Claude Desktop安装

1. 打开Claude Desktop配置目录：
   - macOS: `~/Library/Application Support/Claude/`
   - Windows: `%APPDATA%\Claude\`

2. 编辑`claude_desktop_config.json`：

```json
{
  "mcpServers": {
    "my-skill": {
      "command": "node",
      "args": ["/path/to/my-skill/index.js"]
    }
  }
}
```

3. 重启Claude Desktop

#### 方法2：Claude Code安装

在项目目录下创建`.mcp`配置文件：

```json
{
  "servers": {
    "my-skill": {
      "command": "node",
      "args": ["./skills/my-skill/index.js"]
    }
  }
}
```

### 5.3 调用与测试

安装完成后，在Claude中可以直接调用：

```
你：使用weekly-report技能，帮我生成周报
本周完成了项目A的调研工作
```

Claude会自动识别并使用对应的Skill。

---

## 六、让别人也能使用你的Skills

### 6.1 Skills打包

**准备发布包**：

```
weekly-report-skill/
├── skill.json
├── README.md
├── LICENSE
├── package.json      # 如果是npm包
└── examples/
    └── basic.json
```

**README.md模板**：

```markdown
# Weekly Report Generator Skill

一个用于生成工作周报的MCP Skill。

## 安装方法

1. 下载本仓库
2. 在Claude配置文件中添加...

## 使用方法

...

## 参数说明

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| completed | string | 是 | 本周完成的工作 |

## 示例

...

## 许可证

MIT
```

### 6.2 分享方式

#### 方式1：GitHub分享

1. 创建GitHub仓库
2. 上传Skill文件
3. 添加README说明
4. 分享仓库链接

#### 方式2：npm发布

如果是Node.js实现的Skill：

```bash
# 登录npm
npm login

# 发布
npm publish
```

#### 方式3：Skills市场

未来可能会有专门的Skills市场，类似VS Code插件市场。

### 6.3 版本管理

使用语义化版本号：

```
主版本号.次版本号.修订号

1.0.0 -> 1.0.1  # 修复bug
1.0.0 -> 1.1.0  # 新增功能，保持兼容
1.0.0 -> 2.0.0  # 重大更新，可能不兼容
```

---

## 七、Skills拓展——添加脚本提升功能

### 7.1 为什么需要脚本

纯提示词的Skill能力有限，添加脚本可以：

- 调用外部API
- 处理文件
- 执行计算
- 与系统集成

### 7.2 脚本与Skills的结合

**示例：天气查询Skill**

**skill.json**：
```json
{
  "name": "weather",
  "description": "查询指定城市的天气",
  "version": "1.0.0",
  "parameters": {
    "type": "object",
    "properties": {
      "city": {
        "type": "string",
        "description": "城市名称"
      }
    },
    "required": ["city"]
  },
  "execute": "node weather.js"
}
```

**weather.js**：
```javascript
// 天气查询脚本
const axios = require('axios');

async function getWeather(city) {
  const response = await axios.get(
    `https://api.weather.com/v1?city=${city}`
  );
  return response.data;
}

// MCP标准接口
module.exports = {
  async execute(params) {
    const weather = await getWeather(params.city);
    return {
      content: [{
        type: "text",
        text: `${params.city}的天气：${weather.condition}，温度${weather.temp}°C`
      }]
    };
  }
};
```

### 7.3 实战：增强周报Skill

添加功能：自动保存周报到本地文件

**enhanced-weekly-report.js**：
```javascript
const fs = require('fs');
const path = require('path');

module.exports = {
  async execute(params) {
    // 生成周报内容
    const report = generateReport(params);
    
    // 保存到文件
    const filename = `周报_${getDate()}.md`;
    const filepath = path.join('./reports', filename);
    
    fs.writeFileSync(filepath, report);
    
    return {
      content: [{
        type: "text",
        text: `周报已生成并保存到：${filename}\n\n${report}`
      }]
    };
  }
};

function generateReport(params) {
  return `# 工作周报

## 本周完成
${params.completed}

## 进行中
${params.ongoing || '无'}

## 问题与风险
${params.problems || '无'}

## 下周计划
${params.plans || '待定'}
`;
}

function getDate() {
  const now = new Date();
  return `${now.getFullYear()}${(now.getMonth()+1).toString().padStart(2,'0')}${now.getDate().toString().padStart(2,'0')}`;
}
```

---

## 八、本章小结

### 核心要点回顾

1. **MCP是什么**：AI与外部工具交互的开放协议
2. **Skills是什么**：基于MCP的提示词模板封装
3. **如何创建**：使用AI辅助生成skill.json
4. **如何安装**：配置Claude Desktop或Claude Code
5. **如何分享**：GitHub、npm等方式
6. **如何增强**：添加脚本实现更多功能

### 自检清单

完成本章学习后，问问自己：

- [ ] 我理解MCP协议的作用了吗？
- [ ] 我能解释Skills和提示词模板的区别吗？
- [ ] 我创建了自己的第一个Skill吗？
- [ ] 我知道如何安装和调用Skill吗？
- [ ] 我了解如何分享我的Skill吗？

### 下一步

掌握Skills后，你已准备好进入下一个模块：

**→ [Plugin](04_04_plugin.md)**

在下一模块中，你将学习Plugin的开发和部署，进一步扩展AI的能力边界。

---

*Skills是将你的AI能力标准化的第一步。掌握它，你将能够创建可复用、可分享的AI技能！*
